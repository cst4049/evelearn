
resources:
  roles:
    url: roles
    datasource:
      source: roles
    cache_control: 'max-age=10,must-revalidate'
    cache_expires: 10
    type: dict
    schema:
      $ref: 'https://rawstonedu.com/schema#/definitions/Role/schema'

