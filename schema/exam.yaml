
## resource roles
exams:
  datasource:
    source: exams
  cache_control: 'max-age=10,must-revalidate'
  cache_expires: 10
  ####
  schema:
    name:
      type: string
      required: true
    title:
      type: string
    description:
      type: string
